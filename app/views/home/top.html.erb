<% provide(:title, "ホーム") %>
<div class="center jumbotron">
  <h1>謎解き広場</h1>
  <h3>
      ここは全国の人からクイズを出題してもらえるサイトです 
  </h3>
  <% if !logged_in? %>
  <p><%= link_to "<新規登録はこちら>", signup_path, class: "btn btn-primary" %></p>
  <% end %>
  <%= image_tag("top.png", alt: "クイズの写真", class: "img-fluid") %>
</div>
<div class="row">
  <ul class="col-lg-8 offset-2 list-group">
    <% @posts.each do |post| %>
      <li class="list-group-item list-group-item-action flex-column align-items-start ">
        <%= post.content %>
        <%= image_tag post.image.url if post.image? %>
      </li>
    <% end %>
  </ul>
</div>